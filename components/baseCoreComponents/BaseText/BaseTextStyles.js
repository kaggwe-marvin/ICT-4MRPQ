import { StyleSheet } from "react-native";

export const baseTextStyles = StyleSheet.create({
  baseText: {
    fontSize: 16,
    color: "#333333",
    fontFamily: "",
  },
});

export const headingStyles = StyleSheet.create({
  headingText: {
    fontSize: 24,
    color: "#333333",
    fontWeight: "bold",
    fontFamily: "",
  },
});

export const emphasisStyles = StyleSheet.create({
  emphasisText: {
    fontSize: 16,
    color: "#007AFF",
    fontStyle: "italic",
    fontFamily: "",
  },
});

export const captionStyles = StyleSheet.create({
  captionText: {
    fontSize: 12,
    color: "#666666",
    fontFamily: "Monaco",
  },
});

export const highlightStyles = StyleSheet.create({
  highlightText: {
    fontSize: 16,
    color: "#FFD700",
    fontWeight: "bold",
    fontFamily: "Monaco",
  },
});

export const errorStyles = StyleSheet.create({
  errorText: {
    fontSize: 14,
    color: "#FF0000",
    fontFamily: "Monaco",
  },
});

export const linkStyles = StyleSheet.create({
  linkText: {
    fontSize: 16,
    color: "#007AFF",
    textDecorationLine: "underline",
    fontFamily: "Monaco",
  },
});

export const disabledStyles = StyleSheet.create({
  disabledText: {
    fontSize: 16,
    color: "#999999",
    fontFamily: "Monaco",
  },
});

export const quoteStyles = StyleSheet.create({
  quoteText: {
    fontSize: 16,
    color: "#888888",
    fontStyle: "italic",
    fontFamily: "Monaco",
  },
});

export const successStyles = StyleSheet.create({
  successText: {
    fontSize: 16,
    color: "#00BB00",
    fontWeight: "bold",
    fontFamily: "Monaco",
  },
});

export const smallHeadingStyles = StyleSheet.create({
  smallHeadingText: {
    fontSize: 18,
    color: "#333333",
    fontWeight: "bold",
    fontFamily: "Monaco",
  },
});

export const mutedStyles = StyleSheet.create({
  mutedText: {
    fontSize: 14,
    color: "#999999",
    fontFamily: "Monaco",
  },
});

export const tagStyles = StyleSheet.create({
  tagText: {
    fontSize: 14,
    color: "#FFFFFF",
    backgroundColor: "#333333",
    paddingHorizontal: 8,
    paddingVertical: 4,
    borderRadius: 4,
    fontFamily: "Monaco",
  },
});

export const importantStyles = StyleSheet.create({
  importantText: {
    fontSize: 16,
    color: "#FF0000",
    fontWeight: "bold",
    fontFamily: "Monaco",
  },
});

export const codeStyles = StyleSheet.create({
  codeText: {
    fontSize: 14,
    color: "#333333",
    fontFamily: "Courier",
    backgroundColor: "#F4F4F4",
    padding: 4,
    borderRadius: 4,
  },
});
