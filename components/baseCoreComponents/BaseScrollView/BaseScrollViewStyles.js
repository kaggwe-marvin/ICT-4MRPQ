import { StyleSheet } from "react-native";

export const baseScrollViewStyles = StyleSheet.create({
  baseScrollView: {
    flex: 1,
    backgroundColor: "#ffffff",
    padding: 10,
    borderRadius: 8,
    marginBottom: 15,
    marginTop: 10,
  },
});

export const variant1Styles = StyleSheet.create({
  variantScrollView: {
    backgroundColor: "#f0f0f0",
  },
});

export const variant2Styles = StyleSheet.create({
  variantScrollView: {
    backgroundColor: "#e6e6e6",
  },
});

export const contentContainerStyles = StyleSheet.create({
  contentContainer: {
    flexGrow: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});

export const horizontalScrollStyles = StyleSheet.create({
  horizontalScroll: {
    flexDirection: "row",
    alignItems: "center",
    paddingVertical: 10,
  },
});

export const scrollIndicatorStyles = StyleSheet.create({
  scrollIndicator: {
    backgroundColor: "#333333",
  },
});

export const stickyHeaderStyles = StyleSheet.create({
  stickyHeader: {
    position: "sticky",
    top: 0,
    backgroundColor: "#ffffff",
    zIndex: 999,
  },
});

export const parallaxScrollStyles = StyleSheet.create({
  parallaxScroll: {
    alignItems: "center",
    justifyContent: "center",
  },
});

export const transparentHeaderStyles = StyleSheet.create({
  transparentHeader: {
    backgroundColor: "transparent",
  },
});

export const paginationIndicatorStyles = StyleSheet.create({
  paginationIndicator: {
    width: 8,
    height: 8,
    borderRadius: 4,
    marginHorizontal: 4,
    backgroundColor: "#333333",
  },
});

export const nestedScrollViewStyles = StyleSheet.create({
  nestedScrollView: {
    flex: 1,
    backgroundColor: "#ffffff",
    borderRadius: 8,
    padding: 10,
  },
});

export const stickyFooterStyles = StyleSheet.create({
  stickyFooter: {
    position: "sticky",
    bottom: 0,
    backgroundColor: "#ffffff",
    zIndex: 999,
  },
});

export const gradientBackgroundStyles = StyleSheet.create({
  gradientBackground: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    borderRadius: 8,
  },
});

export const shadowScrollViewStyles = StyleSheet.create({
  shadowScrollView: {
    shadowColor: "#000000",
    shadowOpacity: 0.2,
    shadowOffset: { width: 0, height: 2 },
    elevation: 2,
  },
});

export const zoomableScrollViewStyles = StyleSheet.create({
  zoomableScrollView: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});

export const stickyTabHeaderStyles = StyleSheet.create({
  stickyTabHeader: {
    position: "sticky",
    top: 0,
    backgroundColor: "#ffffff",
    zIndex: 999,
  },
});

export const nestedScrollTabStyles = StyleSheet.create({
  nestedScrollTab: {
    flex: 1,
    backgroundColor: "#ffffff",
    borderRadius: 8,
    padding: 10,
  },
});

export const accordionScrollViewStyles = StyleSheet.create({
  accordionScrollView: {
    paddingHorizontal: 20,
  },
});

export const insetScrollViewStyles = StyleSheet.create({
  insetScrollView: {
    paddingHorizontal: 16,
    paddingVertical: 20,
  },
});

export const stickyActionButtonStyles = StyleSheet.create({
  stickyActionButton: {
    position: "sticky",
    bottom: 20,
    alignSelf: "center",
    zIndex: 999,
  },
});

export const parallaxBackgroundStyles = StyleSheet.create({
  parallaxBackground: {
    flex: 1,
    resizeMode: "cover",
    justifyContent: "center",
    alignItems: "center",
  },
});

export const stickyTabBarStyles = StyleSheet.create({
  stickyTabBar: {
    position: "sticky",
    bottom: 0,
    backgroundColor: "#ffffff",
    zIndex: 999,
  },
});

export const nestedScrollMenuStyles = StyleSheet.create({
  nestedScrollMenu: {
    flex: 1,
    backgroundColor: "#ffffff",
    borderRadius: 8,
    padding: 10,
  },
});

export const gridScrollViewStyles = StyleSheet.create({
  gridScrollView: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "center",
    alignItems: "center",
  },
});

export const stickySectionHeaderStyles = StyleSheet.create({
  stickySectionHeader: {
    position: "sticky",
    top: 0,
    backgroundColor: "#ffffff",
    zIndex: 999,
  },
});

export const pullToRefreshStyles = StyleSheet.create({
  pullToRefresh: {
    backgroundColor: "#f5f5f5",
    padding: 10,
    borderRadius: 8,
  },
});

export const floatingActionButtonStyles = StyleSheet.create({
  floatingActionButton: {
    position: "absolute",
    bottom: 20,
    right: 20,
    backgroundColor: "#007AFF",
    borderRadius: 50,
    width: 60,
    height: 60,
    justifyContent: "center",
    alignItems: "center",
    elevation: 5,
  },
});

export const stickyHeaderWithTabsStyles = StyleSheet.create({
  stickyHeaderWithTabs: {
    position: "sticky",
    top: 0,
    backgroundColor: "#ffffff",
    zIndex: 999,
  },
});

export const sideMenuScrollViewStyles = StyleSheet.create({
  sideMenuScrollView: {
    flex: 1,
    backgroundColor: "#ffffff",
    borderRadius: 8,
    padding: 10,
  },
});

export const pageControlStyles = StyleSheet.create({
  pageControl: {
    flexDirection: "row",
    justifyContent: "center",
    alignItems: "center",
  },
});

export const tiledScrollViewStyles = StyleSheet.create({
  tiledScrollView: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "center",
    alignItems: "center",
  },
});

export const stickyNavHeaderStyles = StyleSheet.create({
  stickyNavHeader: {
    position: "sticky",
    top: 0,
    backgroundColor: "#ffffff",
    zIndex: 999,
  },
});
